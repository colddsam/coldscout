{
  "name": "coldscout-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Cold Scout monorepo — route sync tooling between web (Next.js) and Android (Vite/Capacitor).",
  "scripts": {
    "routes:check": "node web/scripts/sync-routes.mjs --check",
    "routes:sync": "node web/scripts/sync-routes.mjs --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "husky": "^9.1.7"
  }
}
