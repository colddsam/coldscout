# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic[email]==2.9.0
pydantic-settings==2.6.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.13.3

# Task Queue
# ============================================================
# 🔴 CELERY + REDIS — PRESERVED, NOT INSTALLED BY DEFAULT
# Uncomment these when reactivating the Celery approach.
# ============================================================
# celery==5.4.0
redis==5.2.0

# Scheduler
# ✅ ACTIVE — Scheduler (replaces Celery Beat)
apscheduler==3.10.4

# HTTP & Scraping
httpx==0.27.2
beautifulsoup4==4.12.3
playwright==1.48.0
python-whois==0.9.4
dnspython==2.7.0

# AI
groq==0.11.0
google-generativeai>=0.8.3

# Email
aiosmtplib==3.0.1
jinja2==3.1.6

# PDF
reportlab==4.2.5
weasyprint==68.0

# Excel / Reports
openpyxl==3.1.5
matplotlib==3.9.2

# Notifications
python-telegram-bot==21.6
# Web Push (VAPID) — sends notifications to browser & installed PWA service
# workers on every platform. iOS 16.4+ delivers these via APNs internally with
# zero developer setup on our side, which is why this also covers iPhone/iPad
# without an Apple Developer account.
pywebpush==2.0.0
# FCM v1 push for the native Android shell (Capacitor). Imported lazily so the
# server still boots when no Firebase service account is configured.
firebase-admin==6.5.0

# Utilities
loguru==0.7.2
python-dotenv==1.0.1
slowapi==0.1.9
tenacity==9.0.0       # Retry logic
python-multipart==0.0.22
bleach==6.1.0         # XSS Sanitization

# Authentication
bcrypt==4.2.0
python-jose[cryptography]==3.4.0
PyJWT==2.12.0

# Supabase
supabase==2.10.0

# Payment Gateway
razorpay==1.4.2
# razorpay==1.4.2 still imports the legacy ``pkg_resources`` module which
# was removed from default venvs in Python 3.12+ AND was dropped from
# setuptools entirely in 81.x. Pinning ``setuptools<81`` keeps
# ``pkg_resources`` available so the app boots on a fresh 3.12/3.13
# environment without ``ModuleNotFoundError: No module named 'pkg_resources'``.
setuptools<81
